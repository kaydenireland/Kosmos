#version 400 core

in vec3 passColor;
in vec2 passTextureCoord;

out vec4 outColor;

uniform sampler2D tex;

void main() {
    vec4 texColor = texture(tex, passTextureCoord);
    outColor = vec4(vec3(1.0) - texColor.rgb, texColor.a);
}
